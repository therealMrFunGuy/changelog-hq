# GitHub token for private repos and higher rate limits (optional for public repos)
GITHUB_TOKEN=

# LLM endpoint (Ollama-compatible API)
LLM_URL=http://localhost:11435
LLM_MODEL=qwen2.5

# Server port
PORT=8440

# Database path
CHANGELOG_DB_PATH=/data/changelog-hq/changelog.db
